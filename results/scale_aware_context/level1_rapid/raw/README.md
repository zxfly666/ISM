# Level 1 原始评估数据

本目录保留生成评估的 `samples_and_correlations.npz`，以及坐标和 Markov probe 的
逐样本 CSV/JSON。顶层 `level1_rapid/` 只放便于阅读的精选图表与摘要；这里的数据
用于重新统计、改变 bootstrap 设置或核对自动判定。

大体积 CSV/NPZ 使用 Git LFS。文件来自与
`level1_rapid/final/level1_summary.json` 同一次正式运行，未做数值改写。
